global:
  message: welcome to order prod environment
server:
  port: 8085
message: hello fromm order prod!
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/order_prod
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    shoe-sql: true
