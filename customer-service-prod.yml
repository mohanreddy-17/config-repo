global:
  message: welcome to customer service prod environment
server:
  port: 8083
message: hello fromm prod!
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cust_prod
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    shoe-sql: true
